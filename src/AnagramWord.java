import java.util.Scanner;

public class AnagramWord {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        // checks the length if not same then not
        // each character of first word is check in second string hence if found then the word is anagram.
    }
}
